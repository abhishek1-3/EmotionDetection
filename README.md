# EmotionDetection
This project implements a deep learning-based facial emotion recognition system that classifies human emotions from grayscale images using a Convolutional Neural Network (CNN). The model is trained on categorized facial expression datasets and can predict emotions such as Angry, Happy, Sad, Surprise, and more.
